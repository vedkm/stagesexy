---
phase: 01-truthful-laptop-display
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - bitwig-extension/settings.gradle.kts
  - bitwig-extension/build.gradle.kts
  - bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
  - bitwig-extension/src/main/java/com/stagesexy/NormalizedInstrumentPublisher.java
  - bitwig-extension/src/test/java/com/stagesexy/NormalizedInstrumentPublisherTest.java
autonomous: true
requirements:
  - DISP-03
  - LABL-01
must_haves:
  truths:
    - Performer can see the alias-resolved active Instrument Selector label in giant, high-contrast laptop text.
    - Performer can enter a dedicated fullscreen stage mode without introducing a separate desktop shell.
    - Instrument changes propagate through a single truth pipeline fast enough for live switching.
    - The display visibly shows live, stale, or disconnected instead of implying frozen data is current.
    - Stable stage labels come from persisted alias rules, not raw Bitwig names alone.
  artifacts:
    - path: companion/src/types/stage.ts
      provides: Shared normalized event and stage snapshot contracts for the bridge, server, and UI.
    - path: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      provides: Instrument Selector observer bridge entrypoint.
    - path: bitwig-extension/src/main/java/com/stagesexy/NormalizedInstrumentPublisher.java
      provides: Event normalization and local payload emission logic.
    - path: companion/src/state/stage-store.ts
      provides: In-memory truth model with freshness transitions.
    - path: companion/src/labels/alias-store.ts
      provides: Persisted alias resolution for stage labels.
  key_links:
    - from: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      to: companion/src/server/routes/ingest.ts
      via: Local normalized instrument event payloads
      pattern: Extension pushes event-driven updates without polling or UI scraping.
    - from: companion/src/labels/alias-store.ts
      to: companion/src/ui/StageDisplay.tsx
      via: Server-side alias resolution
      pattern: UI renders a stage label that survives raw-name changes.
---

<objective>
Verify and implement the only truly uncertain part of Phase 1: the Bitwig-side observation path for the active `Instrument Selector` layer.

Purpose: prove that the app is watching the real playable layer instead of UI focus or another misleading Bitwig concept.
Output: a buildable Java 21 Bitwig extension that emits normalized local events with a documented layer-key strategy.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-truthful-laptop-display/01-RESEARCH.md
@.planning/phases/01-truthful-laptop-display/01-VALIDATION.md
@.planning/phases/01-truthful-laptop-display/01-01-PLAN.md

<interfaces>
Import the event contract from `companion/src/types/stage.ts` and normalize Bitwig observations into this shape before anything reaches the companion:

```typescript
export interface NormalizedInstrumentEvent {
  source: "bitwig";
  selectorName: string;
  layerKey: string;
  rawName: string;
  sequence: number;
  occurredAt: string;
}
```

If the Bitwig API exposes a durable per-layer identifier, use it for `layerKey`. If not, explicitly emit a documented fallback key based on selector identity plus layer position. Do not key aliases by raw instrument name alone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Java 21 Bitwig extension skeleton and build path</name>
  <files>bitwig-extension/settings.gradle.kts, bitwig-extension/build.gradle.kts, bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java</files>
  <action>Create a minimal Gradle-based Java 21 project that packages the Bitwig extension entrypoint cleanly enough for repeatable local builds. Structure the entrypoint around explicit observer registration for the active `Instrument Selector` chain. Do not add generic support for other Bitwig selector/container types in this phase.</action>
  <verify>
    <automated>cd bitwig-extension && ./gradlew build -x test</automated>
  </verify>
  <done>The extension project builds locally and has a clear entrypoint where the active Instrument Selector observation logic will live.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the observed layer state and prove the alias key strategy</name>
  <files>bitwig-extension/src/main/java/com/stagesexy/NormalizedInstrumentPublisher.java, bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java, bitwig-extension/src/test/java/com/stagesexy/NormalizedInstrumentPublisherTest.java</files>
  <action>Implement the observer callback path so the extension emits a normalized event immediately when the active playable layer changes. Isolate the normalization/publishing logic into a testable class that rejects UI-focus-only signals. The payload must include `selectorName`, `rawName`, `sequence`, `occurredAt`, and a `layerKey` that is either a stable Bitwig-provided ID or the documented fallback `{selector identity}:{layer index}`. Fail fast if the observed object cannot be mapped truthfully; do not silently guess.</action>
  <verify>
    <automated>cd bitwig-extension && ./gradlew test</automated>
  </verify>
  <done>A tested normalization path exists, the emitted payload matches the shared contract, and the alias-key strategy is explicit enough for the companion to persist stage labels safely.</done>
</task>

</tasks>

<verification>
- Keep the extension limited to `Instrument Selector` truth capture.
- Record in the summary which Bitwig observer chain was verified and whether `layerKey` is stable or fallback-based.
</verification>

<success_criteria>
- The Bitwig-side source of truth is executable and buildable instead of assumed.
- Active-layer changes emit normalized payloads immediately, without polling or UI scraping.
- Alias persistence has a non-ambiguous key strategy before companion storage begins.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-02-SUMMARY.md`
</output>
