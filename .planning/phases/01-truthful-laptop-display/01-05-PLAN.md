---
phase: 01-truthful-laptop-display
plan: "05"
type: execute
wave: 1
depends_on:
  - "01-02"
  - "01-04"
files_modified:
  - bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
  - bitwig-extension/src/main/java/com/stagesexy/CompanionIngestClient.java
  - bitwig-extension/src/test/java/com/stagesexy/CompanionIngestClientTest.java
  - bitwig-extension/scripts/test-with-fallback.sh
  - bitwig-extension/Dockerfile.test
  - companion/src/server/main.ts
  - companion/package.json
  - companion/tests/stage-mode.spec.ts
autonomous: true
requirements:
  - DISP-01
  - DISP-03
  - DISP-04
gap_closure: true
must_haves:
  truths:
    - Real Bitwig Instrument Selector changes reach the companion through a production transport path instead of log output.
    - The companion truth service can be started through a normal runtime entrypoint, not only through test helpers.
    - The laptop browser still moves through live, stale, and disconnected states when driven from the real local runtime path.
  artifacts:
    - path: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      provides: Bitwig observer entrypoint wired to a real companion transport.
    - path: bitwig-extension/src/main/java/com/stagesexy/CompanionIngestClient.java
      provides: Local HTTP transport for normalized instrument events.
    - path: companion/src/server/main.ts
      provides: Runnable companion server bootstrap for live use.
    - path: companion/package.json
      provides: Discoverable runtime script for the companion truth service.
    - path: bitwig-extension/scripts/test-with-fallback.sh
      provides: Java 21 verification path with Docker fallback when the toolchain is unavailable locally.
  key_links:
    - from: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      to: companion/src/server/routes/ingest.ts
      via: CompanionIngestClient HTTP POST
      pattern: Normalized events are delivered to /ingest instead of host log output.
    - from: companion/package.json
      to: companion/src/server/main.ts
      via: npm runtime script
      pattern: The truth service starts outside tests on 127.0.0.1:3197 by default.
    - from: companion/src/server/main.ts
      to: companion/src/ui/useStageStream.ts
      via: snapshot and SSE endpoints
      pattern: The browser receives real local server updates through the normal runtime path.
---

<objective>
Close the biggest Phase 1 gap by making the Bitwig observer and the companion runtime path real instead of test-only.

Purpose: restore the promised single truth pipeline from Bitwig -> companion -> laptop browser.
Output: a production ingest transport, a runnable companion server entrypoint, and verification paths that do not rely on synthetic-only setup.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-laptop-display/01-RESEARCH.md
@.planning/phases/01-truthful-laptop-display/01-VERIFICATION.md
@.planning/phases/01-truthful-laptop-display/01-02-SUMMARY.md
@.planning/phases/01-truthful-laptop-display/01-04-SUMMARY.md
@bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
@companion/src/server/app.ts
@companion/src/server/routes/ingest.ts
@companion/tests/stage-mode.spec.ts

<interfaces>
The Bitwig side must keep publishing the existing shared contract into the companion:

```typescript
export interface NormalizedInstrumentEvent {
  source: "bitwig";
  selectorName: string;
  layerKey: string;
  rawName: string;
  sequence: number;
  occurredAt: string;
}
```

The runtime path must continue to terminate at the existing companion endpoints:

```text
POST /ingest
GET /snapshot
GET /events
```

Do not change the selected-track -> first-instrument -> layer-activation truth source or the documented fallback `layerKey` format.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace log-only publishing with a real local companion transport</name>
  <files>bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java, bitwig-extension/src/main/java/com/stagesexy/CompanionIngestClient.java, bitwig-extension/src/test/java/com/stagesexy/CompanionIngestClientTest.java, bitwig-extension/scripts/test-with-fallback.sh, bitwig-extension/Dockerfile.test</files>
  <behavior>
    - Test 1: a normalized Bitwig observation is POSTed to the companion ingest URL instead of being written only to the host log.
    - Test 2: transport failures are surfaced as actionable failures in tests rather than ignored silently.
    - Test 3: the Java verification path still works when local Java 21 is missing by falling back to the repository-owned Docker test image.
  </behavior>
  <action>Introduce a narrow `CompanionIngestClient` that sends the existing normalized event JSON to the local companion `/ingest` endpoint with a short timeout and deterministic default URL. Replace `NormalizedInstrumentPublisher(host::println)` as the production sink in `InstrumentSelectorDisplayExtension` while preserving the verified observer chain and fallback `layerKey` contract from Plan 01-02. Do not add polling, WebSockets, or UI-followed truth paths. Add a repository-owned `bitwig-extension/scripts/test-with-fallback.sh` plus `bitwig-extension/Dockerfile.test` so `./gradlew test` remains the primary path but verification can fall back to Docker when Java 21 is unavailable; the shipped app must still run locally without Docker.</action>
  <verify>
    <automated>cd bitwig-extension && ./scripts/test-with-fallback.sh</automated>
  </verify>
  <done>The Bitwig extension has a real companion transport, transport failures are test-visible, and Java-side verification no longer depends on the machine already having Java 21 installed.</done>
</task>

<task type="auto">
  <name>Task 2: Add a normal companion runtime entrypoint and smoke it through the browser path</name>
  <files>companion/src/server/main.ts, companion/package.json, companion/tests/stage-mode.spec.ts</files>
  <action>Create a companion server bootstrap that starts `startCompanionServer()` on `127.0.0.1:3197` by default and expose it through a discoverable package script for live use. Update the browser smoke path so it starts the companion through that runtime entrypoint instead of only constructing the app in test code, then confirm the laptop UI still shows the incoming label and transitions truthfully to `stale` and `disconnected`. Keep the companion runnable without Docker and keep the laptop-first local binding; phone/LAN behavior stays deferred to Phase 2.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/server/app.spec.ts && npm --prefix companion run test:e2e -- tests/stage-mode.spec.ts</automated>
  </verify>
  <done>The repository has a normal way to start the companion truth service, and automated browser coverage now exercises that runtime path instead of a test-only bootstrap.</done>
</task>

</tasks>

<verification>
- Confirm the source-side verification uses `bitwig-extension/scripts/test-with-fallback.sh` rather than assuming Java 21 already exists locally.
- Run the companion full suite after the runtime entrypoint lands: `npm --prefix companion run test:unit && npm --prefix companion run test:e2e`.
</verification>

<success_criteria>
- The blocker link from Bitwig extension to `POST /ingest` is real, local, and no longer a host-log placeholder.
- The companion truth service can be started intentionally for live use through a normal repository command.
- The laptop browser still reflects live, stale, and disconnected truth when exercised through the real runtime path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-05-SUMMARY.md`
</output>
