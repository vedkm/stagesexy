---
phase: 01-truthful-laptop-display
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - companion/src/ui/StageDisplay.tsx
  - companion/src/ui/stage-display.css
  - companion/src/ui/useFullscreenStageMode.ts
  - companion/src/ui/useFullscreenStageMode.spec.ts
  - companion/src/App.tsx
  - companion/src/main.tsx
autonomous: true
requirements:
  - DISP-01
  - DISP-02
must_haves:
  truths:
    - Performer can see the alias-resolved active Instrument Selector label in giant, high-contrast laptop text.
    - Performer can enter a dedicated fullscreen stage mode without introducing a separate desktop shell.
    - Instrument changes propagate through a single truth pipeline fast enough for live switching.
    - The display visibly shows live, stale, or disconnected instead of implying frozen data is current.
    - Stable stage labels come from persisted alias rules, not raw Bitwig names alone.
  artifacts:
    - path: companion/src/ui/StageDisplay.tsx
      provides: Laptop stage display with dominant label and truthful status treatment.
    - path: companion/src/ui/useFullscreenStageMode.ts
      provides: Browser Fullscreen API controller for stage mode.
    - path: companion/src/App.tsx
      provides: Thin app shell for the dedicated laptop view.
    - path: companion/src/types/stage.ts
      provides: Shared snapshot contract used by the stage display.
  key_links:
    - from: companion/src/ui/useFullscreenStageMode.ts
      to: companion/src/App.tsx
      via: Dedicated stage-mode action
      pattern: The laptop view can enter and exit fullscreen without a separate shell.
    - from: companion/src/types/stage.ts
      to: companion/src/ui/StageDisplay.tsx
      via: `StageSnapshot` props
      pattern: UI remains contract-driven and backend-agnostic until wiring.
---

<objective>
Build the laptop-facing display shell now, independent of live Bitwig transport, so readability and fullscreen behavior are proven before final integration.

Purpose: separate stage-readability work from Bitwig/backend uncertainty and keep the laptop UI intentionally narrow.
Output: a mocked but testable fullscreen stage UI that renders the future snapshot contract truthfully.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-laptop-display/01-RESEARCH.md
@.planning/phases/01-truthful-laptop-display/01-VALIDATION.md
@.planning/phases/01-truthful-laptop-display/01-01-PLAN.md

<interfaces>
The UI should consume the shared snapshot contract directly and stay transport-agnostic in this plan:

```typescript
export interface StageSnapshot {
  selectorName: string;
  layerKey: string | null;
  rawName: string | null;
  displayLabel: string;
  status: "live" | "stale" | "disconnected";
  sequence: number;
  occurredAt: string | null;
  updatedAt: string | null;
}
```

Use mocked snapshots inside `App.tsx` for this plan. Do not invent a second view model.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the dominant stage label and truthful status presentation</name>
  <files>companion/src/ui/StageDisplay.tsx, companion/src/ui/stage-display.css, companion/src/App.tsx</files>
  <action>Create the stage display component that renders one dominant label, a minimal status treatment, and no competing information density. Optimize for a few feet of viewing distance: high contrast, constrained wrapping, and obvious stale/disconnected treatment. Use mocked `StageSnapshot` data in the app shell for now. Do not add animations, dense metadata panels, or phone-responsive layout work.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/ui/StageDisplay.test.tsx</automated>
  </verify>
  <done>The laptop shell renders a giant stage label from the shared snapshot contract and visibly distinguishes live vs stale/disconnected states.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser fullscreen stage mode and shell-level tests</name>
  <files>companion/src/ui/useFullscreenStageMode.ts, companion/src/ui/useFullscreenStageMode.spec.ts, companion/src/App.tsx, companion/src/main.tsx</files>
  <action>Implement a small fullscreen controller around the browser Fullscreen API and wire it into the stage shell. Provide an obvious enter/exit affordance suitable for rehearsal setup. Keep the behavior browser-native; do not add desktop-shell packaging or alternate window-management code paths.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/ui/StageDisplay.test.tsx src/ui/useFullscreenStageMode.spec.ts</automated>
  </verify>
  <done>The app shell can enter and exit fullscreen in tests, and the stage-mode path is ready to be wired to live data later.</done>
</task>

</tasks>

<verification>
- Manual readability rehearsal is deferred until live data wiring exists, but the component should already be visually sparse and high contrast.
- Keep `App.tsx` thin so the final integration plan can swap mock data for the live stream without rewriting the presentation layer.
</verification>

<success_criteria>
- The laptop view is obviously stage-oriented rather than dashboard-oriented.
- Fullscreen behavior works through the browser API and has automated unit coverage.
- Final data wiring can plug into a stable component surface instead of rebuilding the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-03-SUMMARY.md`
</output>
