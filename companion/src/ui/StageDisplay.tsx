import type { StageSnapshot } from "../types/stage";

export interface StageDisplayProps {
  snapshot: StageSnapshot;
}

export function StageDisplay(_props: StageDisplayProps) {
  return <div>Stage display pending implementation.</div>;
}
