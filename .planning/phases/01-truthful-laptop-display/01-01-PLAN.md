---
phase: 01-truthful-laptop-display
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/package.json
  - companion/tsconfig.json
  - companion/vite.config.ts
  - companion/playwright.config.ts
  - companion/index.html
  - companion/src/test/setup.ts
  - companion/src/types/stage.ts
  - companion/src/state/live-state.spec.ts
  - companion/src/state/connection-status.spec.ts
  - companion/src/labels/alias-store.spec.ts
  - companion/src/ui/StageDisplay.test.tsx
  - companion/tests/stage-mode.spec.ts
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - LABL-01
must_haves:
  truths:
    - Performer can see the alias-resolved active Instrument Selector label in giant, high-contrast laptop text.
    - Performer can enter a dedicated fullscreen stage mode without introducing a separate desktop shell.
    - Instrument changes propagate through a single truth pipeline fast enough for live switching.
    - The display visibly shows live, stale, or disconnected instead of implying frozen data is current.
    - Stable stage labels come from persisted alias rules, not raw Bitwig names alone.
  artifacts:
    - path: companion/src/types/stage.ts
      provides: Shared normalized event and stage snapshot contracts for the bridge, server, and UI.
    - path: companion/src/ui/StageDisplay.test.tsx
      provides: Unit coverage for readable label rendering and truthful status treatment.
    - path: companion/tests/stage-mode.spec.ts
      provides: Smoke coverage for fullscreen stage mode behavior.
    - path: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      provides: Instrument Selector observer bridge entrypoint.
    - path: companion/src/state/stage-store.ts
      provides: In-memory truth model with freshness transitions.
    - path: companion/src/labels/alias-store.ts
      provides: Persisted alias resolution for stage labels.
  key_links:
    - from: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      to: companion/src/server/routes/ingest.ts
      via: Local normalized instrument event payloads
      pattern: Extension pushes event-driven updates without polling or UI scraping.
    - from: companion/src/state/stage-store.ts
      to: companion/src/ui/useStageStream.ts
      via: Snapshot plus SSE stream
      pattern: Freshness status changes drive the rendered status badge and fallback behavior.
    - from: companion/src/labels/alias-store.ts
      to: companion/src/ui/StageDisplay.tsx
      via: Server-side alias resolution
      pattern: UI renders a stage label that survives raw-name changes.
---

<objective>
Create the execution foundation for Phase 1 before any Bitwig-specific or UI-specific implementation starts.

Purpose: remove ambiguity by fixing the shared contracts, test harness, and validation commands up front.
Output: a runnable `companion/` workspace, typed truth contracts, and failing tests that encode all Phase 1 behaviors.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-laptop-display/01-RESEARCH.md
@.planning/phases/01-truthful-laptop-display/01-VALIDATION.md

<interfaces>
Create the shared contract file exactly once in `companion/src/types/stage.ts` and make every later plan import from it instead of redefining shapes.

```typescript
export type ConnectionStatus = "live" | "stale" | "disconnected";

export interface NormalizedInstrumentEvent {
  source: "bitwig";
  selectorName: string;
  layerKey: string;
  rawName: string;
  sequence: number;
  occurredAt: string;
}

export interface AliasRecord {
  layerKey: string;
  stageLabel: string;
}

export interface StageSnapshot {
  selectorName: string;
  layerKey: string | null;
  rawName: string | null;
  displayLabel: string;
  status: ConnectionStatus;
  sequence: number;
  occurredAt: string | null;
  updatedAt: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the companion workspace and validation tooling</name>
  <files>companion/package.json, companion/tsconfig.json, companion/vite.config.ts, companion/playwright.config.ts, companion/index.html, companion/src/test/setup.ts</files>
  <action>Create a standalone `companion/` Node 22 + Vite + React + TypeScript workspace with Vitest, React Testing Library, and Playwright configured to match `01-VALIDATION.md`. Add scripts for `dev`, `build`, `test:unit`, and `test:e2e`. Keep the setup laptop-local and browser-first; do not add Electron, phone/LAN binding, or any animation-oriented packages.</action>
  <verify>
    <automated>npm --prefix companion install && npm --prefix companion exec vite --version && npm --prefix companion exec vitest --version && npm --prefix companion exec playwright --version</automated>
  </verify>
  <done>`companion/` installs cleanly, the config files load, and the validation toolchain is present without missing-package or missing-config errors.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the shared truth contracts and encode failing unit tests</name>
  <files>companion/src/types/stage.ts, companion/src/state/live-state.spec.ts, companion/src/state/connection-status.spec.ts, companion/src/labels/alias-store.spec.ts, companion/src/ui/StageDisplay.test.tsx</files>
  <action>Define the normalized bridge and stage-display contracts in `companion/src/types/stage.ts`. Then write failing tests for immediate event application, freshness transitions, alias resolution by `layerKey`, and dominant stage-label rendering. Keep these tests contract-first: they should fail because the production implementation does not exist yet, not because the test harness or imports are broken.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/state/live-state.spec.ts src/state/connection-status.spec.ts src/labels/alias-store.spec.ts src/ui/StageDisplay.test.tsx</automated>
  </verify>
  <done>The unit specs exist, import the shared contracts successfully, and fail on missing implementation expectations rather than setup problems.</done>
</task>

<task type="auto">
  <name>Task 3: Add the fullscreen smoke-test scaffold for the stage path</name>
  <files>companion/tests/stage-mode.spec.ts</files>
  <action>Write a Playwright smoke spec that targets the future laptop stage route and asserts the fullscreen enter/exit path for `DISP-02`. Use deterministic placeholder data and route assumptions that match the shared `StageSnapshot` contract. Keep the scope to laptop fullscreen only; do not introduce phone or join-flow coverage.</action>
  <verify>
    <automated>npm --prefix companion exec playwright test --list tests/stage-mode.spec.ts</automated>
  </verify>
  <done>The fullscreen smoke spec is discoverable by Playwright and is ready to become green once the app shell exists.</done>
</task>

</tasks>

<verification>
- Run `npm --prefix companion run test:unit` after each later code task.
- Treat red tests here as intentional contract locks; later plans are expected to turn them green.
</verification>

<success_criteria>
- The workspace has one authoritative type contract for bridge, server, and UI code.
- All five Phase 1 requirements have an explicit automated test target before implementation starts.
- Later plans can execute without re-deciding stack, file layout, or status semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-01-SUMMARY.md`
</output>
