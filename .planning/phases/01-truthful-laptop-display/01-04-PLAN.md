---
phase: 01-truthful-laptop-display
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - companion/src/server/app.ts
  - companion/src/server/routes/ingest.ts
  - companion/src/server/routes/events.ts
  - companion/src/server/routes/snapshot.ts
  - companion/src/server/app.spec.ts
  - companion/src/state/stage-store.ts
  - companion/src/state/connection-status.ts
  - companion/src/labels/alias-store.ts
  - companion/src/ui/useStageStream.ts
  - companion/src/App.tsx
  - companion/tests/stage-mode.spec.ts
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - LABL-01
must_haves:
  truths:
    - Performer can see the alias-resolved active Instrument Selector label in giant, high-contrast laptop text.
    - Performer can enter a dedicated fullscreen stage mode without introducing a separate desktop shell.
    - Instrument changes propagate through a single truth pipeline fast enough for live switching.
    - The display visibly shows live, stale, or disconnected instead of implying frozen data is current.
    - Stable stage labels come from persisted alias rules, not raw Bitwig names alone.
  artifacts:
    - path: companion/src/server/app.ts
      provides: Fastify boot with local-only HTTP, SSE, and snapshot routes.
    - path: companion/src/state/stage-store.ts
      provides: In-memory truth model fed by normalized Bitwig events.
    - path: companion/src/state/connection-status.ts
      provides: Explicit freshness-state transitions.
    - path: companion/src/labels/alias-store.ts
      provides: Alias persistence with atomic local writes.
    - path: companion/src/ui/useStageStream.ts
      provides: EventSource-based browser subscription to live stage state.
    - path: companion/src/ui/StageDisplay.tsx
      provides: Final laptop rendering of the live snapshot.
  key_links:
    - from: companion/src/server/routes/ingest.ts
      to: companion/src/state/stage-store.ts
      via: Normalized POST payload ingestion
      pattern: Source events update state immediately without polling.
    - from: companion/src/state/stage-store.ts
      to: companion/src/server/routes/events.ts
      via: Snapshot fan-out over SSE
      pattern: Browser clients receive the latest state and status transitions.
    - from: companion/src/labels/alias-store.ts
      to: companion/src/server/routes/snapshot.ts
      via: Alias lookup before serialization
      pattern: The browser never has to resolve raw Bitwig names itself.
    - from: companion/src/server/routes/events.ts
      to: companion/src/ui/useStageStream.ts
      via: EventSource
      pattern: UI freshness and label changes reflect the live server state.
---

<objective>
Integrate the verified Bitwig event source with the laptop UI so Phase 1 ends as a truthful, local-first stage display rather than a collection of mocks.

Purpose: complete the single truth pipeline from normalized ingress to alias-resolved fullscreen display.
Output: a Fastify companion with snapshot/SSE endpoints, persisted aliases, freshness logic, and a browser UI wired to live updates.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-laptop-display/01-RESEARCH.md
@.planning/phases/01-truthful-laptop-display/01-VALIDATION.md
@.planning/phases/01-truthful-laptop-display/01-02-PLAN.md
@.planning/phases/01-truthful-laptop-display/01-03-PLAN.md

<interfaces>
Consume the `NormalizedInstrumentEvent` contract from the Bitwig extension and emit this browser-facing snapshot shape from the companion:

```typescript
export interface StageSnapshot {
  selectorName: string;
  layerKey: string | null;
  rawName: string | null;
  displayLabel: string;
  status: "live" | "stale" | "disconnected";
  sequence: number;
  occurredAt: string | null;
  updatedAt: string | null;
}
```

The companion resolves aliases before publishing snapshots. The browser should render the snapshot, not re-derive status or labels on its own.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the local truth service with freshness and alias persistence</name>
  <files>companion/src/server/app.ts, companion/src/server/routes/ingest.ts, companion/src/server/routes/events.ts, companion/src/server/routes/snapshot.ts, companion/src/server/app.spec.ts, companion/src/state/stage-store.ts, companion/src/state/connection-status.ts, companion/src/labels/alias-store.ts</files>
  <action>Implement the Fastify companion on `127.0.0.1` by default with three core responsibilities: accept normalized Bitwig events, maintain an in-memory truth store with explicit `live -> stale -> disconnected` transitions, and resolve persisted stage-label aliases before serving snapshot/SSE payloads. Persist aliases in a companion-owned local config file using atomic writes. Do not let the server fail open by continuing to present an old instrument as live after updates stop.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/state/live-state.spec.ts src/state/connection-status.spec.ts src/labels/alias-store.spec.ts src/server/app.spec.ts</automated>
  </verify>
  <done>The companion can ingest normalized events, derive truthful freshness status, resolve stage labels, and serve a snapshot/SSE surface suitable for the laptop UI.</done>
</task>

<task type="auto">
  <name>Task 2: Replace mocked UI state with the live EventSource pipeline and close the browser smoke path</name>
  <files>companion/src/ui/useStageStream.ts, companion/src/App.tsx, companion/tests/stage-mode.spec.ts</files>
  <action>Wire the stage app shell to the companion's snapshot/SSE endpoints using `EventSource`. Swap out the mocked snapshot path from Plan 03 while preserving the existing presentation component. Update the Playwright smoke test so it drives the app through the real local ingress path, confirms alias-resolved label updates, verifies fullscreen entry/exit, and proves the UI visibly transitions to `stale` and `disconnected` when the source stops. Keep the browser path laptop-first and local-only; phone mirroring remains deferred.</action>
  <verify>
    <automated>npm --prefix companion run test:e2e -- tests/stage-mode.spec.ts</automated>
  </verify>
  <done>The browser renders real live data from the companion, updates quickly on source changes, and stops implying old data is current when updates cease.</done>
</task>

</tasks>

<verification>
- Run the full suite from `01-VALIDATION.md`: `npm --prefix companion run test:unit && npm --prefix companion run test:e2e`.
- Before phase verification, perform the manual laptop-distance rehearsal described in `01-VALIDATION.md` to confirm real-world readability and truthfulness.
</verification>

<success_criteria>
- Phase 1 ends with a single local truth pipeline: Bitwig extension -> Fastify companion -> laptop EventSource client.
- The performer-facing label is alias-resolved before render and updates near-instantly when the active instrument changes.
- The laptop display never silently presents frozen state as current; it shows live, stale, or disconnected explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-04-SUMMARY.md`
</output>
