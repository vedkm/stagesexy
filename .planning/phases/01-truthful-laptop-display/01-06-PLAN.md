---
phase: 01-truthful-laptop-display
plan: "06"
type: execute
wave: 2
depends_on:
  - "01-05"
files_modified:
  - companion/src/labels/set-alias.ts
  - companion/src/labels/set-alias.spec.ts
  - companion/src/server/app.spec.ts
  - companion/package.json
autonomous: true
requirements:
  - DISP-01
  - LABL-01
gap_closure: true
must_haves:
  truths:
    - A performer can define or update a stable stage label without editing source files, tests, or the alias JSON by hand.
    - Alias writes persist through the companion-owned alias store and affect the next serialized snapshot.
    - The displayed laptop label still comes from server-side alias resolution rather than browser-side remapping.
  artifacts:
    - path: companion/src/labels/set-alias.ts
      provides: User-facing alias command for writing alias records safely.
    - path: companion/package.json
      provides: Discoverable alias management script.
    - path: companion/src/labels/set-alias.spec.ts
      provides: Automated proof that the public alias command validates and persists records.
    - path: companion/src/server/app.spec.ts
      provides: Integration proof that a public alias write changes the companion snapshot label.
  key_links:
    - from: companion/package.json
      to: companion/src/labels/set-alias.ts
      via: npm alias management script
      pattern: Alias writes are available through a normal user-facing command.
    - from: companion/src/labels/set-alias.ts
      to: companion/src/labels/alias-store.ts
      via: setAlias()
      pattern: Public alias writes reuse the existing persisted store instead of bypassing it.
    - from: companion/src/labels/alias-store.ts
      to: companion/src/server/routes/snapshot.ts
      via: server-side resolution before serialization
      pattern: The next snapshot exposes the stage label written through the public command.
---

<objective>
Close the remaining `LABL-01` gap by surfacing a real alias-definition path the performer can use without touching internals.

Purpose: turn alias persistence from an implementation detail into an actual user-facing capability.
Output: a strict alias command, automated validation around it, and proof that public alias writes drive the rendered label.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-laptop-display/01-RESEARCH.md
@.planning/phases/01-truthful-laptop-display/01-VERIFICATION.md
@.planning/phases/01-truthful-laptop-display/01-05-PLAN.md
@.planning/phases/01-truthful-laptop-display/01-04-SUMMARY.md
@companion/src/labels/alias-store.ts
@companion/src/server/app.ts
@companion/src/types/stage.ts

<interfaces>
The public alias write surface should write the same persisted record shape the companion already resolves:

```typescript
export interface AliasRecord {
  layerKey: string;
  stageLabel: string;
}
```

The browser must continue to consume `StageSnapshot.displayLabel` from the server. Do not move alias lookup into React.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface a performer-facing alias command with strict validation</name>
  <files>companion/src/labels/set-alias.ts, companion/src/labels/set-alias.spec.ts, companion/package.json</files>
  <behavior>
    - Test 1: the command accepts a non-empty `layerKey` and `stageLabel` and persists the alias through the existing store.
    - Test 2: blank or missing arguments fail fast with an actionable error message.
    - Test 3: repeated writes for the same `layerKey` replace the prior alias instead of duplicating it.
  </behavior>
  <action>Create a small user-facing alias command that can be run as `npm --prefix companion run alias:set -- --layer-key <value> --stage-label <value>` and implement it on top of the existing alias store. Keep imports at the top of the file, validate arguments explicitly, and fail fast with precise errors instead of silently fixing bad input. Do not add a browser alias editor, phone flow, or manual JSON-editing requirement.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/labels/set-alias.spec.ts src/labels/alias-store.spec.ts</automated>
  </verify>
  <done>A performer can define or replace an alias through a normal repository command, and invalid input is rejected clearly.</done>
</task>

<task type="auto">
  <name>Task 2: Prove public alias writes change the companion snapshot label</name>
  <files>companion/src/server/app.spec.ts, companion/src/labels/set-alias.ts</files>
  <action>Add integration coverage that exercises the public alias command path, then confirms the companion snapshot resolves `displayLabel` from the persisted alias instead of the raw Bitwig name. Reuse the existing server-side serialization flow in `app.ts`; do not introduce browser-side label overrides or test-only shortcuts that bypass the public alias entrypoint.</action>
  <verify>
    <automated>npm --prefix companion run test:unit -- --runInBand src/labels/set-alias.spec.ts src/server/app.spec.ts</automated>
  </verify>
  <done>Alias creation is no longer test-only: a public command writes the alias, the companion snapshot resolves it, and the stage display contract stays server-owned.</done>
</task>

</tasks>

<verification>
- Run the alias-focused suite: `npm --prefix companion run test:unit -- --runInBand src/labels/set-alias.spec.ts src/server/app.spec.ts`.
- Confirm the alias command usage is visible in `companion/package.json` and does not require editing internal files.
</verification>

<success_criteria>
- `LABL-01` is no longer blocked by a missing write path.
- Alias persistence is performer-usable from a normal command surface, not just tests.
- The stage label shown to the browser still comes from server-side alias resolution using the existing snapshot contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-06-SUMMARY.md`
</output>
