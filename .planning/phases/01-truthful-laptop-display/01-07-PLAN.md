---
phase: 01-truthful-laptop-display
plan: "07"
type: execute
wave: 3
depends_on:
  - "01-05"
  - "01-06"
files_modified: []
autonomous: false
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - LABL-01
gap_closure: true
must_haves:
  truths:
    - The performer can trust that the laptop label follows real Bitwig Instrument Selector changes without synthetic ingest calls.
    - The performer can still enter fullscreen stage mode and read the label from performance distance.
    - The display visibly ages from live to stale to disconnected during rehearsal when the source stops.
    - The alias written through the public command appears during real rehearsal instead of the raw Bitwig name.
  artifacts:
    - path: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      provides: Real Bitwig observer transport into the companion runtime.
    - path: companion/src/server/main.ts
      provides: Runnable local truth service for rehearsal.
    - path: companion/src/labels/set-alias.ts
      provides: Public alias-definition command.
    - path: companion/src/App.tsx
      provides: Fullscreen laptop stage surface driven by the live snapshot stream.
  key_links:
    - from: bitwig-extension/src/main/java/com/stagesexy/InstrumentSelectorDisplayExtension.java
      to: companion/src/server/routes/ingest.ts
      via: live Bitwig rehearsal transport
      pattern: Switching layers in Bitwig changes the laptop display without manual POST fixtures.
    - from: companion/src/labels/set-alias.ts
      to: companion/src/ui/StageDisplay.tsx
      via: alias store -> snapshot serialization -> browser render
      pattern: The performer-defined alias appears in the live stage view.
    - from: companion/src/ui/useFullscreenStageMode.ts
      to: companion/src/App.tsx
      via: browser Fullscreen API
      pattern: Stage mode still works during the final trust rehearsal.
---

<objective>
Finish Phase 1 with the human-only trust checks that automation cannot settle: real Bitwig switching, fullscreen readability, and truthful stale/disconnected behavior under rehearsal conditions.

Purpose: confirm the closed gaps actually deliver a trustworthy live laptop display.
Output: explicit human approval or concrete failure notes for the remaining rehearsal-only checks.
</objective>

<execution_context>
@/Users/vedmahadeo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedmahadeo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-truthful-laptop-display/01-VERIFICATION.md
@.planning/phases/01-truthful-laptop-display/01-05-PLAN.md
@.planning/phases/01-truthful-laptop-display/01-06-PLAN.md
@companion/src/App.tsx
@companion/src/ui/useFullscreenStageMode.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the final live-trust rehearsal</name>
  <action>Use the newly added runtime command, alias command, and Bitwig transport to perform the last human-only trust checks for Phase 1. This checkpoint exists because real Bitwig switching, laptop readability from performance distance, and operator trust in stale/disconnected behavior cannot be settled by automation alone.</action>
  <what-built>Real Bitwig -> companion -> laptop runtime transport, a normal companion startup command, and a public alias-definition command.</what-built>
  <how-to-verify>
    1. Run the automated closure suite first: `cd bitwig-extension && ./scripts/test-with-fallback.sh` and `npm --prefix companion run test:unit && npm --prefix companion run test:e2e`.
    2. Start the companion truth service with the new runtime command from `companion/package.json`.
    3. Start the laptop UI and open the stage view in the browser.
    4. Use the public alias command to assign a memorable stage label for the currently expected `layerKey`.
    5. In Bitwig, load the updated extension and switch active `Instrument Selector` layers repeatedly. Do not use manual `POST /ingest` fixtures during this rehearsal.
    6. Confirm the laptop label follows each real switch, shows the alias instead of the raw name, and still enters fullscreen stage mode.
    7. Stand at real performance distance under rehearsal-like lighting and confirm the fullscreen label remains readable without leaning in.
    8. Stop the Bitwig source or companion feed and confirm the display moves from `live` to `stale` to `disconnected` instead of freezing as if current.
  </how-to-verify>
  <resume-signal>Type `approved` if all checks pass, or reply with the exact failed step and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- This plan is intentionally human-gated because Bitwig live switching and stage-distance readability cannot be fully verified through code-only automation.
- If any step fails, reopen `01-VERIFICATION.md` with the observed failure so the next gap plan can target the exact broken truth.
</verification>

<success_criteria>
- A human confirms the label shown on the laptop comes from real Bitwig changes, not synthetic ingest calls.
- Fullscreen readability is accepted at performance distance.
- Stale/disconnected handling is trusted during rehearsal and the performer-defined alias appears in the live display.
</success_criteria>

<output>
After completion, create `.planning/phases/01-truthful-laptop-display/01-07-SUMMARY.md`
</output>
